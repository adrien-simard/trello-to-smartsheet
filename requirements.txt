# Core dependencies for Trello to Smartsheet migration
smartsheet-python-sdk>=3.0.0
openpyxl>=3.1.0

# Development dependencies (optional)
# Uncomment these if you need to build the executable
# pyinstaller>=6.0.0
